// main.js

import { add, multiply } from './mathUtils.js';

const x = 5;
const y = 3;

console.log(`Sum: ${add(x, y)}`);        // Output: Sum: 8
console.log(`Product: ${multiply(x, y)}`); // Output: Product: 15
